# One host per line. Formats: host | user@host | host:port | user@host:port
# Inline # comments and blank lines are ignored.
web01.hostname.loc
db01.hostname.loc
app02.hostname.loc          # currently down
